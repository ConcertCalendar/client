import { keyWordStorage } from "utils/keyWordStorage";

interface searchKeyword {
    chilern ?: React.ReactNode;
    recentKeyword : keyWordStorage;
}

const searchKeyword:React.FC<searchKeyword> = (props) => {
    return (
        <li>

        </li>
        
    )
}